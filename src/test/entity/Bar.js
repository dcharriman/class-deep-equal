export default
class Bar {
  constructor(val = 0) {
    this.val = val
  }

  // static fromObj = obj => {
  //   const newObj = new Bar()
    
  //   for (let prop in obj)  {
  //     newObj[prop] = obj[prop];
  //   }

  //   return newObj
  // }
}